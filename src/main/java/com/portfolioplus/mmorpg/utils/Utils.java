package com.portfolioplus.mmorpg.utils;
public class Utils {
    // Reusable helper method to check if a String is NULL or empty
    public static boolean isNullOrEmpty(String str) {
        return str == null || str.trim().isEmpty();
    }
}
